[project]
name = "soc-llm-lab"
version = "0.1.0"
description = "SOC LLM experimentation lab"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["projects"]
